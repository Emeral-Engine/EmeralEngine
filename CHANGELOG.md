# v0.3の変更点
## 生成物
- 生成物の一部(アセット関連)の処理をランタイムとして切り出しました

## キャラクター
- ロード時に立ち絵が一瞬残ってしまう不具合を修正
- 立ち絵が直前のスクリプトと同じスクリプトをロード時に、立ち絵が読み込まれない不具合を修正

## UI
- フルスクリーン時の余白の位置を上下に均等にするようにしました(生成物)

## 拡張子関連付けに対応
- `project.emeral`をダブルクリックすることによりそのままプロジェクトを開けるように

# データを任意の形式で出力できるようにしました
例えば`json`、`ini`、等々。
